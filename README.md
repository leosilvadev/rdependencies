# API

#### How to run
```bash
cd api

yarn install

# run postgresql
docker-compose up -d

# run db migrations
yarn migration:run

# run api
yarn start:dev
```

# UI

#### How to run
```bash
yarn install

yarn serve
```
